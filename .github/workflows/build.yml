name: Dayanc Web Sitesi Build

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repo Kodlarini Cek
        uses: actions/checkout@v4

      - name: Flutter Kurulumu (Guncel v2)
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Flutter Yapısını Sıfırdan Üret
        run: |
          flutter create --project-name=gnav_web_uygulama --org com.dayanc.web .

      - name: pubspec.yaml Dosyasını Otomatik Yaz
        run: |
          cat << 'EOF' > pubspec.yaml
          name: gnav_web_uygulama
          description: "Dayanc Web Sitesi"
          publish_to: 'none'
          version: 1.0.0+1

          environment:
            sdk: '>=3.0.0 <4.0.0'

          dependencies:
            flutter:
              sdk: flutter
            google_nav_bar: ^5.0.6

          dev_dependencies:
            flutter_test:
              sdk: flutter
            flutter_lints: ^3.0.0

          flutter:
            uses-material-design: true
          EOF

      - name: main.dart Dosyasını Otomatik Yaz
        run: |
          mkdir -p lib
          cat << 'EOF' > lib/main.dart
          import 'package:flutter/material.dart';
          import 'package:google_nav_bar/google_nav_bar.dart';

          void main() {
            runApp(const MaterialApp(home: HomePage()));
          }

          class HomePage extends StatefulWidget {
            const HomePage({super.key});

            @override
            State<HomePage> createState() => _HomePageState();
          }

          class _HomePageState extends State<HomePage> {
            int _selectedIndex = 0;

            final List<Widget> _pages = [
              const Center(child: Text('Web Sitesi Ana Sayfa')),
              const Center(child: Text('Web Sitesi Arama Sayfası')),
            ];

            @override
            Widget build(BuildContext context) {
              return Scaffold(
                body: _pages[_selectedIndex],
                bottomNavigationBar: GNav(
                  color: Colors.red,
                  tabBorder: Border.all(color: Colors.grey, width: 1),
                  tabActiveBorder: Border.all(color: Colors.purple, width: 1),
                  tabBorderRadius: 15,
                  tabBackgroundColor: Colors.white.withOpacity(0.1),
                  selectedIndex: _selectedIndex,
                  onTabChange: (index) {
                    setState(() {
                      _selectedIndex = index;
                    });
                  },
                  tabs: const [
                    GButton(icon: Icons.home, text: 'Ana Sayfa'),
                    GButton(icon: Icons.search, text: 'Arama'),
                  ],
                ),
              );
            }
          }
          EOF

      - name: Kütüphaneleri İndir
        run: flutter pub get

      - name: Web Sitesini Derle
        run: flutter build web --release

      - name: Web Dosyalarını İndirilebilir Yap (build/web)
        uses: actions/upload-artifact@v4
        with:
          name: Dayanc-Web-Sitesi
          path: build/web
          
