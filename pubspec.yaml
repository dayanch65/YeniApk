name: yerel_iletisim_uygulamasi
description: "Dayanc Yerel Ag Telsiz ve SMS Uygulamasi"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sound_stream: ^0.3.0
  permission_handler: ^11.3.1
  network_info_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
