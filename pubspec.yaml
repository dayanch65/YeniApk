name: sesli_arama
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Sesli aralma (mikrofon) kütüphanesi
  flutter_webrtc: ^0.10.4
  # Node.js santraline bağlanma kütüphanesi
  socket_io_client: ^2.0.3

flutter:
  uses-material-design: true
  
